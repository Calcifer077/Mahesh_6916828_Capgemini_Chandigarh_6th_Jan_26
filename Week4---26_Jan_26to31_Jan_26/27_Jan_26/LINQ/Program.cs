namespace LambdaExpressions
{
    internal class Program{


        static void Main(string[] args)
        {
            // int[] numbers = { 1, 2, 3, 4, 5, 6 };

            // var evenNumbers = from num in numbers
            //                 where num % 2 == 0
            //                 select num;

            // foreach (var n in evenNumbers)
            // {
            //     Console.WriteLine(n); // Output: 2, 4, 6
            // }

            // List<string> names = new List<string> { "John", "Alice", "Bob", "Charlie" };

            // var shortNames = names.Where(n => n.Length <= 4);

            // foreach (var name in shortNames)
            // {
            //     Console.WriteLine(name); // Output: John, Bob
            // }

            // int[] numbers = { 10, 20, 30, 40, 50 };

            // var query = from num in numbers
            //             where num > 25
            //             select num;

            // foreach (var n in query)
            // {
            //     Console.WriteLine(n); // Output: 30, 40, 50
            // }

            // string[] names = { "John", "Alice", "Bob" };
            // var query2 = from name in names select name.ToUpper(); // Transform
            // var query3 = from n in nums where n > 2 select n; 


            // foreach (var n in query2)
            // {
            //     Console.WriteLine(n); // Output: 30, 40, 50
            // }

            // object[] items = { "Hello", 123, 45.6, "World" };
            // var strings = items.OfType<string>(); // Only strings
            // var ints = items.OfType<int>(); // Only integers
            // var doubles = items.OfType<double>(); // Only doubles

            // foreach (var n in doubles)
            // {
            //     Console.WriteLine(n); // Output: 30, 40, 50
            // }
        }
    }
}